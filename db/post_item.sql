INSERT INTO wizard (house_name, house_address, city, house_state, zip, img, mortgage_amount, monthlyrent)
VALUES ($1,$2,$3,$4,$5,$6,$7,$8)

-- ALTER TABLE houses
-- ADD COLUMN img VARCHAR(200);

-- -- ALTER TABLE houses
-- -- ADD COLUMN mortgage INT;
-- -- INSERT INTO houses (mortgage)
-- -- VALUES ($7)

-- -- ALTER TABLE houses
-- -- ADD COLUMN rent INT;
-- -- INSERT INTO houses (rent)
-- -- VALUES ($8)